<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sql-query">

    <select id="getStudentById" parameterType="long" resultType="com.mallika.ibatis.model.Student">
        SELECT * FROM student WHERE id = #{id}
    </select>

    <select id="getAllStudents" resultType="com.mallika.ibatis.model.Student">
        SELECT * FROM student
    </select>

    <insert id="createStudent" parameterType="com.mallika.ibatis.model.Student">
        INSERT INTO student (name, department) VALUES (#{name}, #{department})
    </insert>
</mapper>